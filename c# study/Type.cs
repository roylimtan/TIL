using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    //정수 자료형 short int long
    //short(-3만 ~ +3만) 2바이트
    //int(-20억 ~ + 20억) 4바이트 long 8바이트

    float f = 4.00001f;
    double d = 4.000000001;
    decimal m = -4.00001m;
    //float double decimal 실수자료형

    string s = "asdfdsa";
    char c = 'A'; //유니코드가 기록된다.

    //.ToString 숫자를 문자열로 바꿔준다.
    //int.Parse 문자를 숫자로 바꿔준다.



    // Start is called before the first frame update
    void Start()
    {
   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
